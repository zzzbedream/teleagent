# Clave privada del deployer para desplegar en Fuji (testnet).
# SOLO una billetera de testnet dedicada, nunca una con fondos reales.
# Formato: 0x... (64 caracteres hex). Foundry carga este archivo automáticamente.
DEPLOYER_PRIVATE_KEY=
